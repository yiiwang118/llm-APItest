{
  "name": "llm-api-test-studio",
  "version": "0.1.0",
  "description": "A local web studio for testing multiple LLM provider APIs with streaming output.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check src/providers.js && node --check public/app.js"
  }
}
